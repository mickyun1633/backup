-- 2번 : DML(data 조작어) 기초

-- table 생성(DDL)
CREATE TABLE  TBL_MEMBER(
-- 컬럼명 자료형(용량)
	MEMBER_ID NUMBER,
	MEMBER_NAME VARCHAR2(100),
	MEMBER_AGE NUMBER(3)
);

--- DML연습
SELECT * FROM TBL_MEMBER;

--- data 추가
-- INSERT INTO 테이블명(컬럼명1, 컬럼명2) VALUES(값1, 값2),
-- INSERT INTO 테이블명 VALUES(값1, 값2, 값3);

INSERT INTO TBL_MEMBER 
VALUES(1, '짱구', 5);

INSERT INTO TBL_MEMBER(MEMBER_ID, MEMBER_NAME)
VALUES(2, '유리');

INSERT INTO TBL_MEMBER 
VALUES(3, '철수', NULL);

SELECT * FROM TBL_MEMBER;

SELECT *
FROM TBL_MEMBER
WHERE MEMBER_NAME = '짱구';

-- 데이터 삭제
DELETE FROM TBL_MEMBER;

SELECT * FROM TBL_MEMBER;

DELETE FROM TBL_MEMBER
WHERE MEMBER_ID = 2;

-- 나이가 NULL인 행만 삭제
DELETE FROM TBL_MEMBER 
WHERE MEMBER_AGE IS NULL;

-- 추가 짱구, 철수, 유리, 훈이 4명 회원 추가
INSERT INTO TBL_MEMBER
--VALUES(2, '철수', 5);
--VALUES(3, '유리', 5);
VALUES(4, '훈이', 5);

SELECT * FROM TBL_MEMBER
ORDER BY MEMBER_ID;

-- 수정 
-- UPDATE 테이블명 SET 기존컬럼명 = 변경할값 [WHERE 조건];

-- MEMBER_ID 1인값을 20으로 변경
UPDATE TBL_MEMBER
SET MEMBER_ID = 20
WHERE MEMBER_NAME = '짱구';

SELECT * FROM TBL_MEMBER;

--- 짱구 20

--- 철수 21
UPDATE TBL_MEMBER
SET MEMBER_ID = 21
WHERE MEMBER_NAME = '훈이';

SELECT * FROM 





CREATE TABLE TBL_STUDENT (
	STUDENT_NUMBER NUMBER,
	STUDENT_NAME VARCHAR2(100),
	STUDENT_MATH NUMBER,
	STUDENT_ENG NUMBER,
	STUDENT_KOR NUMBER,
	STUDENT_GRADE VARCHAR2(100)
);

DROP TABLE TBL_STUDENT;


SELECT * FROM TBL_STUDENT;


INSERT INTO TBL_STUDENT(STUDENT_NUMBER, STUDENT_NAME, STUDENT_MATH, STUDENT_ENG, STUDENT_KOR)
--VALUES(1, '짱구', 90, 90, 90);
--VALUES(2, '철수', 100, 80, 100);
--VALUES(3, '유리', 70, 25, 55);
--VALUES(4, '훈이', 40, 100, 70);
VALUES(5, '맹구', 70, 77, 79);


SELECT * FROM TBL_STUDENT;




























